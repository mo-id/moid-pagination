"use strict";
Object.defineProperty(exports, "__esModule", { value: !0 });
var e = require("react");
function a(e, a) {
  for (var t = [], r = e; r <= a; r++) t.push(r);
  return t;
}
exports.usePagination = function (t, r, n) {
  return {
    previousPage: e.useMemo(
      function () {
        return Math.max(1, t.page - 1);
      },
      [t]
    ),
    nextPage: e.useMemo(
      function () {
        return Math.min(t.page + 1, t.totalPages);
      },
      [t]
    ),
    getPathForPage: e.useCallback(
      function (e) {
        var a = new URL(n);
        return (
          a.searchParams.set("pagina", String(e)), "" + a.pathname + a.search
        );
      },
      [n]
    ),
    isCurrentPage: e.useCallback(
      function (e) {
        return t.page === e;
      },
      [t]
    ),
    visiblePages: e.useMemo(
      function () {
        var e = t.page,
          n = t.totalPages;
        return n <= r
          ? a(1, Math.min(n, r))
          : e <= Math.ceil(r / 2)
          ? a(1, r)
          : e >= n - Math.floor(r / 2)
          ? a(n - r + 1, n)
          : a(e - Math.floor(r / 2), e + Math.floor(r / 2));
      },
      [t]
    ),
  };
};
//# sourceMappingURL=pagination.cjs.production.min.js.map
