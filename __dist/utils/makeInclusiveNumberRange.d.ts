/**
 * Crea un array di numeri che vanno dal parametro `from` al parametro `to` inclusi.
 * E.g. from: 2, to: 5 => [2, 3, 4, 5]
 */
export declare function makeInclusiveNumberRange(
  from: number,
  to: number
): number[];
